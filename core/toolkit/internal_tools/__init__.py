"""
Internal (in-process) tool implementations.

These tools run inside the ScannerEngine process via the InternalTool protocol.
They are registered in core/toolkit/registry.py as ToolDefinitions with
tool_type="internal".
"""

