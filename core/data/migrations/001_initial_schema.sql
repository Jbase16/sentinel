-- Migration 001: Initial Schema
-- This migration creates the base tables for SentinelForge.
-- NOTE: This is just a reference migration - actual tables are created by Database._create_tables()
-- Future migrations will modify these tables.

-- This file intentionally left mostly empty as a placeholder.
-- Real migrations will add columns, indexes, foreign keys, etc.

-- Example of what a real migration might look like:
-- ALTER TABLE findings ADD COLUMN confidence REAL DEFAULT 0.0;
-- CREATE INDEX idx_findings_confidence ON findings(confidence);
