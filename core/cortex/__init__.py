"""Cortex package.

We keep this file intentionally minimal.

Note: Historically, some `core/*` subdirectories were implicit namespace packages
(no `__init__.py`). Adding this file makes import behavior more predictable
across tools (pytest, type checkers, packaging).
"""

