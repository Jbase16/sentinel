# Archive

This directory contains historical development tools and scripts that are no longer actively used but are preserved for reference.

## dev-tools/

One-off scripts used during codebase refactoring and cleanup:

- `add_all_comments.py` - Batch header comment addition tool
- `audit_top_level_dirs_*.py` - Directory structure audit scripts
- `cleanup_stage_one.py` - Initial cleanup script
- `debug_imports.py` - Import debugging utility
- `fix_dataclass_imports*.py` - Dataclass import fixing scripts
- `repro_import.py` - Import issue reproduction script

These tools were used during specific development phases and are kept for historical reference only.
